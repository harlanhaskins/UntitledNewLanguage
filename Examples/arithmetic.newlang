func multiply(_ a: Int, _ b: Int) -> Int {
    return a * b
}

func subtract(_ x: Int, _ y: Int) -> Int {
    return x - y
}

func complex(_ x: Int) -> Int {
    var temp: Int = x * 2
    temp = temp + 5
    return temp - 1
}